server:
  base_url: "http://24.124.32.70:38703/completion"
  model_alias: "remote-gpu-flash"

workload:
  users: 15
  duration_seconds: 300
  ramp_up_seconds: 30
  seed: 42

prompts:
  min_tokens: 50
  max_tokens: 200
  prefix: "Explain briefly: "

prometheus:
  enabled: false

client:
  timeout_seconds: 180
  connect_timeout: 30
  retries: 5
  backoff_factor: 2.0
